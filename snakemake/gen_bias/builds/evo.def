Bootstrap: docker
From: pytorch/pytorch:2.7.1-cuda12.6-cudnn9-devel

%post
    pip install flash-attn==2.8.0.post2 --no-build-isolation --break-system-packages
    pip install evo2 --break-system-packages

%environment
    export PATH="/root/.local/bin:$PATH"
