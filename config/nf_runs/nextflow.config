nextflow.enable.dsl=2
singularity.enabled = true
resume = true
workDir = "../nf-work"

executor {
    queueSize = 10
    submitRateLimit = "10 sec"
}

process {
    executor = "slurm"
    clusterOptions = "--qos=cpu24h"
    memory = "30 GB"
}
